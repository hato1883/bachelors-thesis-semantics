\documentclass[xcolor={dvipsnames,table}]{beamer}

%  Include preamble
\input{preamble/imports.tex}
\input{preamble/macros.tex}

\input{preamble/lean-style.tex}

% Choose a theme (optional, but makes it look pretty)
% \usetheme{Madrid}
\usetheme{metropolis}
\usecolortheme{default}

\title{Mechanizing Proofs from a Course on Programming Language Semantics in Lean}
\subtitle{Bachelors Thesis Presentation}
\author{Hampus Toft}
\date{26th May 2026}%\today

\begin{document}

% Slide 1: Title Page
\begin{frame}
  \titlepage
\end{frame}

% Slide 2: Introduction
\begin{frame}
  \section*{Mechanizing Proofs from a Course on Programming Language Semantics in Lean}  
  \begin{block}<2->{Mechanization}
    \vspace{1pt}
    Formal proof in a computer proof assistant.
  \end{block}

  \begin{block}<3->{Programming Language Semantics}
    \vspace{1pt}
    The meaning behind the code.
  \end{block}

  \begin{block}<4->{But why?}
    \vspace{1pt}
    To find any similarities between pen-and-paper and mechanized proofs.
  \end{block}

\end{frame}

% Slide 3: Introduction (Nielson)
\begin{frame}{Introduction}
  \section*{Nielson \& Nielson's Framework}
  Case study using "\textbf{Semantics with Applications: An Appetizer}". \\
  With the focus on:
  \begin{itemize}
    \item<1-> Chapter 1: The \While language
    \item<2-> Chapter 2: Operational Semantics
      \begin{itemize}
        \item<3-> Natural Semantics
        \item<4-> Structural Operational Semantics
      \end{itemize}
  \end{itemize}
\end{frame}

% Slide 4: Introduction (Lean)
\begin{frame}{Introduction}
  \section*{The Lean 4 Proof Assistant}
  Uses Dependent Type Theory. \\
  With features such as:
  \begin{itemize}
    \item<2-> Inductive Types
    \item<3-> Tactics
    \item<4-> Strict Correctness
  \end{itemize}
\end{frame}

% Slide 5: Research Questions
\begin{frame}
  \section*{Research Questions}
  \begin{itemize}
    \item<1-> \textbf{RQ1:} Do the proof techniques presented in "\emph{Semantics with Applications: An Appetizer}"
    map directly to Lean's internal mechanisms, or do they require significant
    structural reformulation?

    \item<2-> \textbf{RQ2:} What implicit assumptions or "pedagogical shortcuts"
    present in "\emph{Semantics with Applications: An Appetizer}" must be made explicit to satisfy the Lean proof assistant?
  \end{itemize}
\end{frame}

% Slide 6: Method
\begin{frame}
    \section{Method}
    \begin{enumerate}
        \item Find a something to mechanize
        \item Then mechanize it
        \item Put side-by-side
        \item Repeat Steps 1-3
        \item \dots
        \item Profit?
    \end{enumerate}
\end{frame}

% Slide 7: Mechanization of Aexp
\begin{frame}[fragile]{Evaluation process: \alert{Syntactic Definitions}}
    \begin{columns}[T]
        \begin{column}{0.55\textwidth}
            \begin{block}{Method}
\begin{enumerate}
    \item<1-> Find something to mechanize
    \item<2-> Then mechanize it
    \item<3-> Put them side-by-side
    \item<5-> Repeat Steps 1-3
\end{enumerate}
            \end{block}
        \end{column}
        \begin{column}{0.45\textwidth}
            \begin{block}{Checklist}
\begin{itemize}
    \item Syntactic Definitions \No
    \item Semantic Functions \No
    \item Inference Rules \No
    \item Theorems and Proofs \No
\end{itemize}
            \end{block}
        \end{column}
    \end{columns}

    % \vspace{0.5cm}
    
    % We wrap the side-by-side view inside an overprint or conditional block 
    % so it only shows up from Slide 1 onwards, changing state dynamically.
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            % Show right column on slide 2 (isolated) and slides 3+ (side-by-side)
            \begin{block}<2->{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4]
inductive Aex§\tikzmarknode{ex7_ln1}{p}§ where
  | num (n : Num) §\tikzmarknode{ex7_ln2}{}§
  | var (x : Var)
  | add (a1 a2 : Aexp)
  | mul (a1 a2 : Aexp)
  | sub (a1 a2 : Aexp)
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Only show left column from slide 1, 3, and 4 (Skip slide 2 to isolate Lean)
            \only<1->{
                \begin{block}{Original Definition}
\[
\begin{array}{lcl}
    \\[0.3em]
    \tikztarget{ex7_txt1}{a} &::=& \tikztarget{ex7_txt2}{n} \\ 
        &\mid& x \\ 
        &\mid& a_1 + a_2 \\ 
        &\mid& a_1 \star a_2 \\ 
        &\mid& a_1 - a_2 \\
\end{array}
\]
                \end{block}
            }
        \end{column}
        
    \end{columns}

    % --- TIKZ OVERLAY LAYER ---
    % This executes "on top" of the rendered page. We use slide tracking (<3->) 
    % to control exactly when individual arrows appear.
    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        % Slide 3: Show the first alignment arrow (Subgroup -> Structure definition)
        \path<3-> [draw, ->]
            (ex7_txt1.north)
            [bend right=30] to
            node[below, sloped, text=black, font=\tiny]
                {Type being defined}
            (ex7_ln1.north);
        
        % Slide 4: Show the second alignment arrow with a descriptive label
        \path<4-> [draw, ->, draw=red]
            (ex7_txt2.west)
            [bend left=20] to
            node[below, sloped, text=black, font=\tiny]
                {Inductive Definition}
            (ex7_ln2.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 8: Mechanization of Var
\begin{frame}[fragile]{Evaluation process: \alert{Syntactic Definitions}}

    % \vspace{0.5cm}
    
    % We wrap the side-by-side view inside an overprint or conditional block 
    % so it only shows up from Slide 1 onwards, changing state dynamically.
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            % Show right column on slide 2 (isolated) and slides 3+ (side-by-side)
            \begin{block}<2->{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4]
def Var := String
deriving instance
  DecidableEq for Var
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Only show left column from slide 1, 3, and 4 (Skip slide 2 to isolate Lean)
            \only<1->{
                \begin{block}{Original Definition}
\vspace{1pt}
We assume that the structure of variables is given elsewhere;
for example, variables might be strings
of letters and digits starting with a letter.
                \end{block}
            }
        \end{column}
        
    \end{columns}
\end{frame}

% Slide 9: Aexp_eval without Notation
\begin{frame}[fragile]{Evaluation process: \alert{Semantic Functions}}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}<2->{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
def Aexp_eval :
  Aexp §\(\to\)§ (State §\(\to\)§ §\(\mathbb{Z}\)§)
  | .num n, _ => Num_eval n §\tikzmarknode{ex9_ln1}{}§
  | .var x, s => s x
  | .add §\(a_1\ a_2\)§, s => Aexp_eval §\(a_1\)§ s
                   + Aexp_eval §\(a_2\)§ s
  | .mul §\(a_1\ a_2\)§, s => Aexp_eval §\(a_1\)§ s 
                   * Aexp_eval §\(a_2\)§ s
  | .sub §\(a_1\ a_2\)§, s => Aexp_eval §\(a_1\)§ s
                   - Aexp_eval §\(a_2\)§ s
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            \begin{block}<1->{Original Definition}
\scriptsize
\[
\mathcal{A}: \mathbf{Aexp} \to (\mathbf{State} \to \mathbb{Z})
\]
\[
\begin{array}{lcl}
        \tikztarget{ex9_txt1}{}\mathcal{A}[\![n]\!]s &=& \mathcal{N}[\![n]\!] \\
        \mathcal{A}[\![x]\!]s &=& s \, x \\
        \mathcal{A}[\![a_1 + a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s + \mathcal{A}[\![a_2]\!]s \\
        \mathcal{A}[\![a_1 \star a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s \cdot \mathcal{A}[\![a_2]\!]s \\
        \mathcal{A}[\![a_1 - a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s - \mathcal{A}[\![a_2]\!]s
\end{array}
\]
            \end{block}
        \end{column}
        
    \end{columns}

    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<3> [draw, ->]
            (ex9_txt1.north)
            [bend right=5] to
            (ex9_ln1.north);
        
    \end{tikzpicture}
\end{frame}

% Slide 10: Notation
\begin{frame}[fragile]{Evaluation process: \alert{Semantic Functions}}
    If we now add custom notation using
    \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
infixl:65 " + " => While.Aexp.add
infixl:70 " §\color{purple}\(\star\)§ " => While.Aexp.mul
infixl:65 " - " => While.Aexp.sub

set_option quotPrecheck false in
set_option hygiene false in
notation "§\color{purple}\(\mathcal{N}[\![\)§" n "§\color{purple}\(]\!]\)§" => Num_eval n

set_option quotPrecheck false in
set_option hygiene false in
notation "§\color{purple}\(\mathcal{A}[\![\)§" a "§\color{purple}\(]\!]\)§" s:max => Aexp_eval a s

-- + a lot more for everything else that gets defined.
    \end{lstlisting}
\end{frame}

% Slide 11: Semantic functions Aexp_eval
\begin{frame}[fragile]{Evaluation process: \alert{Semantic Functions}}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
def Aexp_eval :
  Aexp §\(\to\)§ (State §\(\to\)§ §\(\mathbb{Z}\)§)
  | .num n, _ => §\(\mathcal{N}[\![\)§n§\(]\!]\)§
  | .var x, s => s x
  | §\(a_1 + a_2\)§, s => §\(\mathcal{A}[\![a_1]\!]\)§s + §\(\mathcal{A}[\![a_2]\!]\)§s
  | §\(a_1 \star a_2\)§, s => §\(\mathcal{A}[\![a_1]\!]\)§s * §\(\mathcal{A}[\![a_2]\!]\)§s
  | §\(a_1 - a_2\)§, s => §\(\mathcal{A}[\![a_1]\!]\)§s - §\(\mathcal{A}[\![a_2]\!]\)§s
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            \begin{block}{Original Definition}
\scriptsize
\[
\mathcal{A}: \mathbf{Aexp} \to (\mathbf{State} \to \mathbb{Z})
\]
\[
\begin{array}{lcl}
        \\
        \mathcal{A}[\![n]\!]s &=& \mathcal{N}[\![n]\!] \\
        \mathcal{A}[\![x]\!]s &=& s \, x \\
        \mathcal{A}[\![a_1 + a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s + \mathcal{A}[\![a_2]\!]s \\
        \mathcal{A}[\![a_1 \star a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s \cdot \mathcal{A}[\![a_2]\!]s \\
        \mathcal{A}[\![a_1 - a_2]\!]s &=& \mathcal{A}[\![a_1]\!]s - \mathcal{A}[\![a_2]\!]s
\end{array}
\]
            \end{block}
        \end{column}
        
    \end{columns}
\end{frame}

% Slide 12: Inference rules
\begin{frame}[fragile]{Evaluation process: \alert{Inference Rules} (NS)}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}<2->{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
inductive big_step : Stmt §\(\to\)§
  State §\(\to\)§ State §\(\to\)§ Prop where
  | ass {s x a} : §\tikzmarknode{ex12_ln1}{}§
      §\(\langle\)§x §\(: \equiv\)§ a, s§\(\rangle\)§ §\(\to_{ns}\)§ (s[x §\mapsto§ §\(\mathcal{A}[\![a]\!]\)§ s]) §\tikzmarknode{ex12_ln2}{}§
                \end{lstlisting}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
  | if_true {b S§$_{1}$§ S§$_{2}$§ s s'}
    (h_cond : §\(\mathcal{B}[\![b]\!]\)§ s = §\tikzmarknode{ex12_ln5}{tt}§)
    (h_then : §\(\langle\)§S§\(_1\)§, s§\(\rangle\)§ §\(\to_{ns}\)§ §\tikzmarknode{ex12_ln4}{s'}§) :
    §\(\langle\)§§if§ b §then§ S§$_{1}$§ §else§ S§$_{2}$§, s§\(\rangle\)§ §\(\to_{ns}\)§ s' §\tikzmarknode{ex12_ln6}{}§
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Only show left column from slide 1, 3, and 4 (Skip slide 2 to isolate Lean)
            \begin{block}<1->{Original Definition}
\vspace{1em}
\scriptsize
\[
  \begin{prooftree}
    \hypo{} % Empty hypothesis for an axiom
    \infer[
      left label=\tikzmarknode{ex12_txt1}{}[ass$_{\text{ns}}$]
      ]1{
      \tikzmarknode{ex12_txt2}{}\langle x := a, s \rangle \to s[x \mapsto \mathcal{A}[\![a]\!] s]
      }
  \end{prooftree}
\]
\vspace{2em}
\[
  \begin{prooftree}
    \hypo{\tikzmarknode{ex12_txt4}{}\langle S_1, s \rangle \to s'}
    \infer[
      left label={
        \parbox{0.8cm}{
          [if$_{\text{ns}}^{\text{\ tt}}$]\\
          \tikzmarknode{ex12_txt5}{}$\mathcal{B}[\![b]\!]s$ \\
          $= \mathbf{tt}$}
        }
      ]1{
        \tikzmarknode{ex12_txt6}{}\langle \mathtt{if}\ b\ \mathtt{then}\ S_1\ \mathtt{else}\ S_2, s \rangle \to s'
      }
  \end{prooftree}
\]
            \end{block}
        \end{column}
    \end{columns}


    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2-3> [draw, ->]
            (ex12_txt1.west)
            to
            (ex12_ln1.east);
        \path<3> [draw, ->]
            (ex12_txt2.west)
            to
            (ex12_ln2.east);

        \path<5-7> [draw, ->, draw=red]
            (ex12_txt4.west)
            [bend right=45] to
            (ex12_ln4.east);
        \path<6-7> [draw, ->, draw=red]
            (ex12_txt5.west)
            [bend right=15] to
            (ex12_ln5.east);
        \path<7> [draw, ->, draw=red]
            (ex12_txt6.west)
            [bend left=20] to
            (ex12_ln6.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 13: Semantic functions S_ns
\begin{frame}[fragile]{Evaluation process: \alert{Semantic Functions}}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}<2->{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
--           s' if S terminates
-- §\color{green!40!black}\(\mathcal{S}_{ns}[\![\)§S§\color{green!40!black}\(]\!]\)§ = { in natural semantics
--           else undefined
noncomputable
def S_ns (S : Stmt) (s : State) :
  Part State :=
  { Dom := §\(\exists\)§ s', §\(\langle\)§S, s§\(\rangle\)§ §\(\to_{ns}\)§ s',
    get := fun h => Classical.choose h }
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            \begin{block}<1->{Original Definition}
\scriptsize
\[
\begin{array}{lcl}
        \mathcal{S}_{ns}: \mathbf{Stmt} \to (\mathbf{State} \hookrightarrow \mathbf{State})
        \\[0.8em]
        \mathcal{S}_{ns}[\![S]\!]s = \begin{cases}
            s' & if \langle S, s \rangle \to s'\\
            undef & otherwise
        \end{cases}
\end{array}
\]
            \end{block}
        \end{column}
        
    \end{columns}
\end{frame}

% Slide 14: Lemma 2.5 definition 1/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 1/6}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
theorem lemma_2_5
  (b : Bexp) (S : Stmt)
  (s s' : State) :
  (§\(\langle\)§§while§ b §then§ S, s§\(\rangle\)§ §\(\to_{ns}\)§ s') §\tikzmarknode{ex14_ln1}{}§
  §\(\leftrightarrow\)§ §\tikzmarknode{ex14_ln2}{}§
  (§\(\langle\)§§if§ b
    §then§ (S; §while§ b §then§ S)
    §else§ Stmt.skip, s§\(\rangle\)§ §\tikzmarknode{ex14_ln3}{}§
  §\(\to_{ns}\)§ s') := by
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Second half of definition
            \only{
                \begin{block}{Original Definition}
\vspace{0.4em}
\small
\textbf{Lemma 2.5}\\
\vspace{0.4em}
\scriptsize
The statement
\[\tikzmarknode{ex14_txt1}{}\mathtt{while}\ b\ \mathtt{do}\ S\]
is \tikzmarknode{ex14_txt2}{}semantically equivalent to
\[\tikzmarknode{ex14_txt3}{}\mathtt{if}\ b\ \mathtt{then} (S;\ \mathtt{while}\ b\ \mathtt{do}\ S)\ \mathtt{else}\ \mathtt{skip}\]
                \end{block}
            }
        \end{column}
        
    \end{columns}
    

    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2> [draw, ->]
            (ex14_txt1.west)
            [bend right=5] to
            (ex14_ln1.east);
            
        \path<3> [draw, ->]
            (ex14_txt2.west)
            [bend right=5] to
            (ex14_ln2.east);
            
        \path<4> [draw, ->]
            (ex14_txt3.west)
            [bend right=5] to
            (ex14_ln3.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 15: Lemma 2.5 equiv stmt 2/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 2/6}

    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
  apply Iff.intro

  -- Forward direction:
  -- if => while
  case mp => §\tikzmarknode{ex15_ln1}{}§
    exact if_compact b S s s'

  -- Backward direction:
  -- while => if
  case mpr => §\tikzmarknode{ex15_ln2}{}§
    exact while_expand b S s s'
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Second half of definition
            \begin{block}{Original Definition}
\vspace{0.8em}
\scriptsize
The proof is in \tikzmarknode{ex15_txt1}{}two parts
            \end{block}
        \end{column}
    \end{columns}


    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2> [draw, ->]
            (ex15_txt1.south)
            [bend left=10] to
            (ex15_ln1.east);
            
        \path<2> [draw, ->]
            (ex15_txt1.south)
            [bend left=30] to
            (ex15_ln2.east);
        
    \end{tikzpicture}

\end{frame}

% Slide 16: Lemma 2.5 while_expand 3/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 3/6}
    
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
theorem while_expand
  (b : Bexp) (S : Stmt)
  (s s'' : State) :
  (§\(\langle\)§§while§ b §then§ S, s§\(\rangle\)§ §\(\to_{ns}\)§ s'') §\tikzmarknode{ex16_ln1}{}§
  §\(\to\)§ §\tikzmarknode{ex16_ln2}{}§
  (§\(\langle\)§§if§ b
    §then§ (S; §while§ b §then§ S) §\tikzmarknode{ex16_ln3}{}§
    §else§ Stmt.skip, s§\(\rangle\)§
  §\(\to_{ns}\)§ s'') := by
  intro h_while §\tikzmarknode{ex16_ln4}{}§
  cases h_while with §\tikzmarknode{ex16_ln5}{}§
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Second half of definition
            \only{
                \begin{block}{Original Definition}
\vspace{0.8em}
\scriptsize
We shall first prove that if
\[
\tikzmarknode{ex16_txt1}{} \langle \mathtt{while}\ b\ \mathtt{do}\ S,\ s\rangle \to s'' \tag{*}
\]
\tikzmarknode{ex16_txt2}{}then
\[
  \parbox{4cm}{
    \tikzmarknode{ex16_txt3}{} $\langle \mathtt{if}\ b\ \mathtt{then} (S;\ \mathtt{while}\ b\ \mathtt{do}\ S)$\\
    $\mathtt{else}\ \mathtt{skip},\ s\rangle \to s''$
  } \tag{**}
\]
\tikzmarknode{ex16_txt4}{} Because (*) holds, we know that we have a derivation tree T for it.\\
\tikzmarknode{ex16_txt5}{}It can have one of two forms depending on whether it has been constructed using the
rule [while$_{\text{ns}}^{\text{\ tt}}$] or the axiom [while$_{\text{ns}}^{\text{\ ff}}$].
                \end{block}
            }
        \end{column}
        
    \end{columns}


    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2> [draw, ->]
            (ex16_txt1.west)
            [bend right=5] to
            (ex16_ln1.east);

        \path<3> [draw, ->]
            (ex16_txt2.west)
            [bend right=5] to
            (ex16_ln2.east);

        \path<4> [draw, ->]
            (ex16_txt3.west)
            [bend right=5] to
            (ex16_ln3.east);

        \path<5> [draw, ->]
            (ex16_txt4.west)
            [bend right=5] to
            (ex16_ln4.east);

        \path<6> [draw, ->]
            (ex16_txt5.west)
            [bend left=10] to
            (ex16_ln5.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 17: Lemma 2.5 while_expand 4/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 4/6}
    
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
  | while_true
    h_cond_true T§$_1$§ T§$_2$§ =>
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Second half of definition
            \only{
                \begin{block}{Original Definition}
\vspace{0.8em}
\scriptsize
In the first case, the derivation tree T has the form
\[
    \begin{prooftree}
        \hypo{T_1}
        \hypo{T_2}
        \infer[
            ]2{
            \langle \mathtt{while}\ b\ \mathtt{do}\ S,\ s\rangle \to s''
            }
    \end{prooftree}
\]
where \(T_1\) is a derivation tree with root \(\langle S,\ s\rangle \to s'\) and \(T_2\) is a derivation tree
with root \(\langle \mathtt{while}\ b\ \mathtt{do}\ S,\ s'\rangle \to s''\).
Furthermore, \(\mathcal{B}[\![b]\!]s = \mathbf{tt}\).
                \end{block}
            }
        \end{column}
        
    \end{columns}
\end{frame}

% Slide 18: Lemma 2.5 while_expand 5/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 5/6}
    
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
    have h_comp :
      (§\(\langle\)§S; while b then S, s§\(\rangle\)§
      §\(\to_{ns}\)§ s'') :=
        big_step.comp T§$_1$§ T§$_2$§ §\tikzmarknode{ex18_ln1}{}§
                \end{lstlisting}

                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
    exact big_step.if_true §\tikzmarknode{ex18_ln3}{}§
      h_cond_true -- Condition §\tikzmarknode{ex18_ln2}{}§
      h_comp      -- Then case
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Second half of definition
            \only{
                \begin{block}{Original Definition}
\vspace{0.8em}
\scriptsize
Using the derivation trees \(T_1\) and \(T_2\) as the premises for the rules [comp$_{\text{ns}}$], we can construct the derivation tree
\[\tikzmarknode{ex18_txt1}{}
    \begin{prooftree}
        \hypo{T_1}
        \hypo{T_2}
        \infer[
            % left label=[comp$_{\text{ns}}$]
            ]2{
            \langle S;\ \mathtt{while}\ b\ \mathtt{do}\ S,\ s\rangle \to s''
            }
    \end{prooftree}
\]
Using that \tikzmarknode{ex18_txt2}{} \(\mathcal{B}[\![b]\!]s = \mathbf{tt}\), we can use the rule \tikzmarknode{ex18_txt3}{} [if$_{\text{ns}}^{\ \text{tt}}$]
to construct the derivation tree
\[
  \begin{prooftree}
    \hypo{T_1}
    \hypo{T_2}
    \infer2{
      \langle S;\ \mathtt{while}\ b\ \mathtt{do}\ S,\ s\rangle \to s''
    }
    \infer1{
      \parbox{4cm}{$
        \langle \mathtt{if}\ b\ \mathtt{then}\ (S;\ \mathtt{while}\ b\ \mathtt{do}\ S)\ \\
        \mathtt{else}\ \mathtt{skip},\ s\rangle \to s''
      $}
    }
  \end{prooftree}
\]
hereby showing that (**) holds.
                \end{block}
            }
        \end{column}
        
    \end{columns}

    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2> [draw, ->]
            (ex18_txt1.west)
            [bend right=5] to
            (ex18_ln1.east);
            
        \path<3> [draw, ->]
            (ex18_txt2.west)
            [bend right=5] to
            (ex18_ln2.east);
            
        \path<4> [draw, ->]
            (ex18_txt3.west)
            [bend right=5] to
            (ex18_ln3.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 19: Lemma 2.5 final stmt 6/6
\begin{frame}[fragile]{Evaluation process: \alert{Proofs} (NS) 6/6}

    (**) to (*) (reverse direction)\\
    Same pattern:
    \begin{enumerate}
      \item Split into cases
      \item Use premises to construct new trees
      \item Tell lean that new tree is wanted tree
    \end{enumerate}
\end{frame}

% §\tikzmarknode{ex20_ln1}{}§
% \tikzmarknode{ex20_txt1}{}
% Slide 20: if false contradiction
\begin{frame}[fragile]{Can't dismiss all obvious cases}
\section*{Example from Theorem 2.9}
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
  | if_true §\tikzmarknode{ex20_ln1}{}§
      h_cond_true §\tikzmarknode{ex20_ln3}{}§ h_deriv_then §\tikzmarknode{ex20_ln4}{}§
      ih_then =>
    intro h_deriv_alt §\tikzmarknode{ex20_ln2}{}§
    cases h_deriv_alt with
    | if_true §\tikzmarknode{ex20_ln5}{}§
        _ h_deriv_then_alt §\tikzmarknode{ex20_ln6}{}§ =>
      exact §\tikzmarknode{ex20_ln7}{}§ ih_then _
             h_deriv_then_alt

    | if_false §\tikzmarknode{ex20_ln8}{}§
        h_cond_false_alt _ =>
      rw [h_cond_true] at
          h_cond_false_alt
      contradiction
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Only show left column from slide 1, 3, and 4 (Skip slide 2 to isolate Lean)
            \begin{block}<1-9>{Original Definition}
\vspace{0.2em}
\scriptsize
\tikzmarknode{ex20_txt1}{} \textbf{The case} [if$_{\text{ns}}^{\ \text{tt}}$]: \\
Assume that
\\\(
\tikzmarknode{ex20_txt2}{} \langle \mathtt{if}\ b\ \mathtt{then}\ S_1\ \mathtt{else}\ S_2,\ s \rangle \to s'
\)\\
holds because
\\\(
\tikzmarknode{ex20_txt3}{} \mathcal{B}[\![b]\!] = \mathbf{tt}\ \mbox{and}\ \tikzmarknode{ex20_txt4}{} \langle S_1,\ s \rangle \to s'
\)\\
\tikzmarknode{ex20_txt5}{} From \(\mathcal{B}[\![b]\!] = \mathbf{tt}\) we get that the only rule that could be applied to give the
alternative \(\langle \mathtt{if}\ b\ \mathtt{then}\ S_1\ \mathtt{else}\ S_2,\ s \rangle \to s''\) is 
[if$_{\text{ns}}^{\ \text{tt}}$]. So it must be the case that
\\\(
\tikzmarknode{ex20_txt6}{}\langle S_1,\ s \rangle \to s''
\)\\
\tikzmarknode{ex20_txt7}{} But then the induction hypothesis can be applied to the premise \(\langle S_1,\ s \rangle \to s'\)
and from \(\langle S_1,\ s \rangle \to s''\) we get \(s' = s''\).
            \end{block}
        \end{column}
    \end{columns}

    \begin{tikzpicture}[remember picture, overlay, >=Stealth, thick, cyan]
        
        \path<2> [draw, ->]
            (ex20_txt1.west)
            [bend right=5] to
            (ex20_ln1.east);
            
        \path<3> [draw, ->]
            (ex20_txt2.west)
            [bend right=5] to
            (ex20_ln2.east);
            
        \path<4> [draw, ->]
            (ex20_txt3.west)
            [bend right=5] to
            (ex20_ln3.east);
            
        \path<5> [draw, ->]
            (ex20_txt4.west)
            [bend right=5] to
            (ex20_ln4.east);
            
        \path<6> [draw, ->]
            (ex20_txt5.west)
            [bend right=5] to
            (ex20_ln5.east);
            
        \path<7> [draw, ->]
            (ex20_txt6.west)
            [bend right=5] to
            (ex20_ln6.east);
            
        \path<8> [draw, ->]
            (ex20_txt7.west)
            [bend right=5] to
            (ex20_ln7.east);
            
        \path<9> [draw, ->, red]
            (ex20_txt5.west)
            [bend right=5] to
            (ex20_ln8.east);
        
    \end{tikzpicture}
\end{frame}

% Slide 21: Theorem 2.26
\begin{frame}[fragile]{Not all proofs are easy}
\section*{Example from Theorem 2.26}
    \begin{columns}[T]
        
        % --- LEFT COLUMN: Lean Code ---
        \begin{column}{0.50\textwidth}
            \begin{block}{Lean 4 Formalization}
                \begin{lstlisting}[style=lean4, basicstyle=\scriptsize\ttfamily]
  | while_false h_cond =>
    apply small_step_star.step
    case step =>
      apply small_step.while_unroll
    case rest =>
      exact small_step_star.step
        (small_step.if_false h_cond)
        (small_step_star.step
          small_step.skip
          small_step_star.refl)
                \end{lstlisting}
            \end{block}
        \end{column}

        % --- RIGHT COLUMN: Original Text Target---
        \begin{column}{0.48\textwidth}
            % Only show left column from slide 1, 3, and 4 (Skip slide 2 to isolate Lean)
            \begin{block}{Original Definition}
\vspace{1em}
\scriptsize
\textbf{The case} [while$_{\text{ns}}^{\ \text{ff}}$]: Straightforward.
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

% Slide 22: Conclusion time
\begin{frame}
    \section*{Conclusion}
    \begin{itemize}
        \item<1-> Direct mapping is possible
        \begin{itemize}
        \item<2-> BUT\dots using notation increases the similarity
        \end{itemize}
        \vspace{1em}
        \item<3-> Types are generally easy
        \begin{itemize}
        \item<4-> BUT\dots some are harder (Var)
        \end{itemize}
        \vspace{1em}
        \item<5-> General proof ideas can be kept
        \begin{itemize}
        \item<6-> BUT\dots needs careful evaluation of omitted cases
        \end{itemize}
    \end{itemize}

\end{frame}

% Slide 23: Conclusion RQ
\begin{frame}{Conclusion}
  \begin{itemize}
    \item<1-> \textbf{RQ1:} Do the proof techniques presented in "\emph{Semantics with Applications: An Appetizer}"
    map directly to Lean's internal mechanisms, or do they require significant
    structural reformulation?
    \begin{itemize}
        \item<2-> \textbf{Answer}: They have a direct mapping, but needs notation and careful constructions.
    \end{itemize}
    \vspace{1em}
    \item<3-> \textbf{RQ2:} What implicit assumptions or "pedagogical shortcuts"
    present in "\emph{Semantics with Applications: An Appetizer}" must be made explicit to satisfy the Lean proof assistant?
    \begin{itemize}
        \item<4-> \textbf{Answer}: "Straightforward" might not be as straightforward as you may think.
    \end{itemize}
  \end{itemize}
\end{frame}

% Slide 24: Thank you
\begin{frame}
    \section*{Thank you}
    \only<2->{Questions?}

\end{frame}
\end{document}