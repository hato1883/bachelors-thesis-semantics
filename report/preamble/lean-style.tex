% --- lean-style.tex ---

% REMOVED: inputenc and \DeclareUnicodeCharacter. 
% LuaLaTeX handles these natively through fontspec.

% Define the lean4 style
\lstdefinestyle{lean4}{
  language=bash, % Fallback
  basicstyle=\small\ttfamily\color{black},
  keywordstyle=\small\ttfamily\bfseries\color{blue!70!black},
  commentstyle=\small\itshape\color{green!40!black},
  stringstyle=\color{purple},
  numberstyle=\footnotesize\sffamily\color{gray},
  aboveskip=1ex,
  belowskip=1ex,
  tabsize=2,
  columns=fullflexible,
  xleftmargin=1ex,
  showstringspaces=false,
  % Lean 4 Keywords
  morekeywords={
    def, theorem, lemma, example, open, import, 
    variable, variables, instance, class, structure, 
    inductive, syntax, macro, deriving, if, then, else, 
    let, have, show, by, match, with, infix, notation, namespace, end
  },
  % Line breaks
  breaklines=true,                % Enables wrapping
  postbreak=\mbox{\textcolor{gray}{$\hookrightarrow$}\space}, % Symbol at start of wrapped line
  breakatwhitespace=true,         % Prevents breaking in the middle of words
  breakindent=20pt,               % Indents the wrapped portion for visual clarity
  % Comments
  morecomment=[l]{--},
  morecomment=[s]{/-}{-/},
  % Symbols and Delimiters
  escapeinside=@@,
  % Unicode Handling for listings
  % Even with LuaLaTeX, 'literate' ensures symbols look perfect in code
  extendedchars=true,
  literate=
    {→}{{\ensuremath{\to}}}1 
    {⇒}{{\ensuremath{\Rightarrow}}}1 
    {∀}{{\ensuremath{\forall}}}1 
    {∃}{{\ensuremath{\exists}}}1 
    {λ}{{\ensuremath{\lambda}}}1 
    {:=}{{$:=$}}2 
    {∘}{{\ensuremath{\circ}}}1 
    {∣}{{\ensuremath{\mid}}}1
}

% code block command usage: \begin{Lean_Code} def x := 1 \end{Lean_Code}
\lstnewenvironment{Lean_Code}[1][style=lean4]{\lstset{#1}}{}
\lstnewenvironment{Lean_Code_Numbered}[1][style=lean4, numbers=left]{\lstset{#1}}{}

% Inline command usage: \lean{def x := 1}
\newcommand{\lean}[1]{\lstinline[style=lean4]@#1@}